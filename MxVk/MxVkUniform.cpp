#include "MxVkUniform.h"

namespace Mix {
    namespace Graphics {
        /*void UniformMgr::init(std::shared_ptr<Core> core, std::shared_ptr<DeviceAllocator> allocator) {
            mCore = core;
            mAllocator = allocator;
        }

        Id UniformMgr::createRenderableObj() {
            std::shared_ptr<Buffer> uniform = createUniformBuffer<MeshUniform>();
            mRenderableObjs.emplace(mRenderableObjId.now(), uniform);
            return mRenderableObjId.next();
        }

        Id UniformMgr::createCamera() {
            std::shared_ptr<Buffer> uniform = createUniformBuffer<CameraUniform>();
            mCameras.emplace(mCameraId.now(), uniform);
            return mCameraId.next();
        }*/
    }
}
